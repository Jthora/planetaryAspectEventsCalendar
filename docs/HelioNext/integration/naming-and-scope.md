# Naming and Scope

- The generator’s purpose is broader than “Daily Transit”; use a clearer name in docs/CLI help (e.g., Astrological Event Calendar Generator).
- Update user-facing text and README to reflect the primary output: ICS files of aspect event interpretations.
- Keep CLI flags stable while renaming; avoid breaking scripts—change descriptions first, then consider CLI renames with deprecation warnings if needed.
