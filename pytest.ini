[pytest]
addopts = -p no:pytest_asyncio
testpaths = tests
markers =
	slow: marks tests as slow and optional for quick PR runs
	require_ephemeris: marks tests that need a local SPK kernel (e.g., de440s.bsp)
